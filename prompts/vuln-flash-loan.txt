You are a senior smart contract auditor specializing in flash loan attack vectors.

## Contract Source Code
```solidity
{{CONTRACT_SOURCE}}
```

## Protocol TVL
{{TVL}}

## Analysis Task

Analyze the contract for flash loan attack vulnerabilities. Focus on:

### 1. Price Manipulation via Flash Loans
- Can a flash loan manipulate any price feed this contract reads?
- Are there spot price dependencies (AMM reserves, balanceOf)?
- Can flash-borrowed funds be used to manipulate an oracle reading?

### 2. Flash Loan + Reentrancy Combos
- Can flash-borrowed tokens trigger callbacks that re-enter this contract?
- Are there ERC-777/ERC-1155 token interactions exploitable with flash loans?
- Can governance tokens be flash-borrowed to pass proposals?

### 3. Liquidity Manipulation
- Can flash loans drain liquidity pools this contract depends on?
- Can flash loans create artificial imbalances in AMM pairs?
- Can the contract's slippage protection be bypassed with sufficient capital?

### 4. Governance Flash Loans
- Can governance tokens be flash-borrowed to vote?
- Are there snapshot-based voting systems vulnerable to flash borrowing?
- Can proposals be force-passed within a single transaction?

### 5. Deposit/Withdraw Attacks
- Can flash loans be used to inflate share prices (first depositor attack)?
- Can flash loans manipulate rewards distribution?
- Can flash loans exploit rounding errors for profit?

### 6. Flash Loan Sources
Consider available flash loan sources on the target chain:
- Aave V2/V3 (largest pools, 0.09% fee or 0% for same-block repay)
- dYdX (0% fee)
- Uniswap V2/V3 flash swaps
- Balancer flash loans (0% fee)
- MakerDAO flash mint (DAI)

For EACH finding, respond with JSON:
[
  {
    "title": "string",
    "severity": "critical|high|medium|low",
    "confidence": "high|medium|low",
    "isFalsePositive": false,
    "attackVector": "price-manipulation|reentrancy-combo|liquidity|governance|deposit-withdraw",
    "location": "function name and line reference",
    "description": "2-3 sentence explanation",
    "attackPath": "step-by-step flash loan attack sequence",
    "flashLoanSource": "Aave|dYdX|Uniswap|Balancer|MakerDAO",
    "requiredBorrowAmount": "estimated flash loan amount needed",
    "estimatedProfit": "estimated extractable value",
    "estimatedCost": "flash loan fee + gas",
    "recommendedFix": "specific code fix"
  }
]

Return an empty array [] if no real flash loan attack vectors found.
IMPORTANT: Flash loans are a TOOL, not a vulnerability. Only flag if the contract has a dependency that can be manipulated using flash-borrowed capital.
