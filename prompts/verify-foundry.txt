You are a smart contract exploit developer tasked with creating a Foundry-based proof of concept.

## Vulnerability Details
{{VULNERABILITY}}

## Target Contract
- Address: {{CONTRACT_ADDRESS}}
- Chain: {{CHAIN}}
- RPC URL: {{RPC_URL}}

## Task

Write a complete Foundry test that proves the vulnerability is exploitable on a mainnet fork.

### Requirements

1. **Fork Setup**
   - Use `vm.createSelectFork(RPC_URL)` to fork mainnet at latest block
   - Import all necessary interfaces and contracts
   - Set up the attacker address with `vm.startPrank(attacker)`

2. **Pre-Exploit State**
   - Record balances and state BEFORE the exploit
   - Assert initial conditions are as expected
   - Log initial state for verification

3. **Exploit Execution**
   - Implement the exact attack steps described in the vulnerability
   - Handle flash loan setup if needed
   - Include all intermediate steps (approvals, swaps, etc.)

4. **Post-Exploit Verification**
   - Assert the attacker profited (balance increased)
   - Calculate and log the extracted value
   - Verify the protocol was actually impacted (TVL decreased, state corrupted, etc.)

5. **Compilation Requirements**
   - Must compile against the actual contract bytecode on mainnet
   - Use `vm.deal()` for ETH funding, not actual transfers
   - Keep gas limit reasonable (< 30M gas)

### Output Format

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "forge-std/Test.sol";

// Add required interfaces here

contract ExploitTest is Test {
    // Target contract instances
    // Attacker address

    function setUp() public {
        vm.createSelectFork("{{RPC_URL}}");
        // Setup code
    }

    function testExploit() public {
        // Pre-exploit state
        uint256 balanceBefore = ...;

        // Execute exploit
        vm.startPrank(attacker);
        // ... exploit steps ...
        vm.stopPrank();

        // Verify exploit succeeded
        uint256 balanceAfter = ...;
        uint256 profit = balanceAfter - balanceBefore;

        assertGt(profit, 0, "Exploit should be profitable");
        emit log_named_uint("Profit (wei)", profit);
        emit log_named_uint("Profit (USD approx)", profit / 1e18 * ethPrice);
    }
}
```

Respond with the complete Solidity test file only. No explanatory text.
